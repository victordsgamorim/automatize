/**
 * @automatize/core-settings
 * Platform-agnostic settings dialog context and hook accessors
 */

export * from './types';
export {
  SettingsContext,
  useSettings,
  useSettingsSafe,
} from './SettingsContext';
export type { SettingsContextValue } from './types';
