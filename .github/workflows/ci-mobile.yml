name: Mobile Build

on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
    paths:
      - 'apps/mobile/**'
      - 'core/**'
      - 'integration/**'
      - 'packages/**'
  pull_request:
    paths:
      - 'apps/mobile/**'
      - 'core/**'
      - 'integration/**'
      - 'packages/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '20'

jobs:
  android-prebuild:
    name: Android Prebuild
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate native projects
        run: |
          cd apps/mobile
          npx expo prebuild --platform android --clean
        env:
          EXPO_NO_TELEMETRY: 1

      - name: Upload Android project
        uses: actions/upload-artifact@v4
        with:
          name: android-project
          path: apps/mobile/android
          retention-days: 1

  android-build:
    name: Android Build (Debug)
    runs-on: ubuntu-latest
    needs: android-prebuild
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Android project
        uses: actions/download-artifact@v4
        with:
          name: android-project
          path: apps/mobile/android

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('apps/mobile/android/**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Fix gradlew permissions
        run: chmod +x apps/mobile/android/gradlew

      - name: Build Android Debug APK
        run: |
          cd apps/mobile/android
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-debug-apk
          path: apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7

  android-lint:
    name: Android Lint
    runs-on: ubuntu-latest
    needs: android-prebuild
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Android project
        uses: actions/download-artifact@v4
        with:
          name: android-project
          path: apps/mobile/android

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('apps/mobile/android/**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Fix gradlew permissions
        run: chmod +x apps/mobile/android/gradlew

      - name: Run Android Lint
        run: |
          cd apps/mobile/android
          ./gradlew lint

  ios-prebuild:
    name: iOS Prebuild
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate native projects
        run: |
          cd apps/mobile
          npx expo prebuild --platform ios --clean
        env:
          EXPO_NO_TELEMETRY: 1

      - name: Patch Podfile (remove FlipperConfiguration for RN 0.73+)
        run: |
          node -e "
            const fs = require('fs');
            const path = 'apps/mobile/ios/Podfile';
            let content = fs.readFileSync(path, 'utf8');
            // Remove the comment block about react-native-flipper
            content = content.replace(/^# If you are using a .react-native-flipper.*\n(#.*\n)*/m, '');
            // Remove the flipper_config variable block (from 'flipper_config =' to the closing 'end')
            content = content.replace(/^flipper_config = .*\n(.*\n)*?^end\n/m, '');
            // Remove the :flipper_configuration line from use_react_native!
            content = content.replace(/\s*:flipper_configuration => flipper_config\n/, '\n');
            fs.writeFileSync(path, content);
            console.log('Podfile patched: FlipperConfiguration references removed');
          "

      - name: Upload iOS project
        uses: actions/upload-artifact@v4
        with:
          name: ios-project
          path: apps/mobile/ios
          retention-days: 1

  ios-build:
    name: iOS Build (Debug)
    runs-on: macos-latest
    needs: ios-prebuild
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download iOS project
        uses: actions/download-artifact@v4
        with:
          name: ios-project
          path: apps/mobile/ios

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            apps/mobile/ios/Pods
            ~/.cocoapods
          key: ${{ runner.os }}-cocoapods-${{ hashFiles('apps/mobile/ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-cocoapods-

      - name: Install CocoaPods
        run: |
          cd apps/mobile/ios
          pod install
        env:
          COCOAPODS_DISABLE_STATS: true

      - name: Build iOS (xcodebuild)
        run: |
          cd apps/mobile/ios
          xcodebuild -workspace Automatize.xcworkspace -scheme Automatize -configuration Debug -destination "platform=iOS Simulator,name=iPhone 15" build
        env:
          CI: true

      - name: Upload build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ios-build-log
          path: ~/Library/Developer/Xcode/DerivedData
          retention-days: 7

  mobile-typecheck:
    name: Mobile TypeScript Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run typecheck
        run: pnpm turbo run typecheck --filter=@automatize/mobile
