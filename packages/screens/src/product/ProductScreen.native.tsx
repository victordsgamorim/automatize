import React from 'react';
import { View } from 'react-native';
import { Text } from '@automatize/ui';
import type { ProductScreenProps } from './ProductScreen.types';

export const ProductScreen: React.FC<ProductScreenProps> = () => {
  return (
    <View>
      <Text>Product list screen not yet implemented for native.</Text>
    </View>
  );
};
