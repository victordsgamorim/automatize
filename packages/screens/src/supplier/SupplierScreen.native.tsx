import React from 'react';
import { View } from 'react-native';
import { Text } from '@automatize/ui';
import type { SupplierScreenProps } from './SupplierScreen.types';

export const SupplierScreen: React.FC<SupplierScreenProps> = () => {
  return (
    <View>
      <Text>Supplier screen not yet implemented for native.</Text>
    </View>
  );
};
